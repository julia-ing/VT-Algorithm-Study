1. Linked List
  Use reference value
  
3. Graph, Tree
   Check each Tree level
   Swapping values in python : `l, r = r, l`
   Deque: O(1), List : O(n)
   Null check : `if root == None` , `if not root`
   